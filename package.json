{
  "name": "bitbucket-cloner",
  "version": "1.0.0",
  "description": "Script to clone all repositories from a BitBucket workspace, separating them by Project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas Freitas",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1"
  }
}
